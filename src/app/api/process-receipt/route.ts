// Task 3.2 implementará el procesamiento OCR con Claude Vision
// Este archivo es un placeholder para establecer la ruta correcta
import { NextResponse } from "next/server";

export async function POST() {
  return NextResponse.json(
    { error: "Endpoint en construcción — Task 3.2" },
    { status: 501 }
  );
}
