// Task 1.2 implementará el Google Login con Supabase Auth
// Este archivo es un placeholder para establecer la ruta correcta
export default function LoginPage() {
  return (
    <main className="flex min-h-dvh flex-col items-center justify-center px-6">
      <h1 className="text-2xl font-bold tracking-tight" style={{ color: "var(--color-accent)" }}>
        Debt Tracker
      </h1>
      <p className="mt-2 text-sm" style={{ color: "var(--color-text-secondary)" }}>
        Autenticación en construcción — Task 1.2
      </p>
    </main>
  );
}
